---
ansible_become_pass: sudo_pass

# Docker
ignore_running_containers: false

# Kubernetes
disable_firewall: false
disable_swap: false
k8s_master: cpu-server1
k8s_nodes:
  - cpu-server2
  - gpu-server1
  - gpu-server2
reset_k8s: false
isolate_k8s_master: false
